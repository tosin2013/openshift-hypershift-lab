apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../aws-base

labels:
  - pairs:
      app.kubernetes.io/instance: aws-prod
      environment: prod

patches:
  - path: domain-patch.yaml
